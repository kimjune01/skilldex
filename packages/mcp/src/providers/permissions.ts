/**
 * Re-export access level types for provider manifests.
 * These match the types defined in the API's integration-permissions.ts
 */

export type AccessLevel = 'read-write' | 'read-only' | 'disabled' | 'none';
