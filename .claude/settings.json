{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'SKILLOMATIC_DEPLOY=1'; then exit 0; elif echo \"$CLAUDE_TOOL_INPUT\" | grep -qE 'sst\\s+deploy'; then echo 'Use the /deploy slash command instead of running sst deploy directly. It handles change detection and selective targeting.'; exit 2; fi"
          }
        ]
      }
    ]
  }
}
